# 2-78 页面设计确认区移动到底部

## 本次目标

将第 04 步页面设计中的“本地保存 / 确认并进入下一步”操作区移动到页面最底部。

## 已完成

- `PageDesignView.vue` 中 `context-handoff` 区块已从页面区域下方移动到 `LlmStepRevisionPanel` 之后。
- 大模型修改面板在前，最终确认进入下一步在最后，符合先修改后确认的操作顺序。

## 验证点

- 页面底部最后一个主要操作区应为“确认并进入下一步”。
- 原保存和确认逻辑未变，仍会自动保存本页关键内容到 `page.md`。
