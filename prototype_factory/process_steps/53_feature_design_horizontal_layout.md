# 2-53 功能设计页横向布局优化

## 目标

根据第 03 步功能设计页面截图，调整业务场景、当前场景详情、功能模块映射和大模型修改区的布局关系。

## 已完成

- `业务场景` 改为横向卡片布局，最多四列展示，窄屏自动换行。
- `当前场景` 移到业务场景下方，并将关联角色、任务流程、建议页面横向展开展示。
- `功能模块映射` 改为横向 Flex 排布，模块内容较多时自动换行。
- 大模型修改面板移到确认按钮上方。
- `确认并进入下一步` 保留在页面底部右侧，继续触发功能设计结果写入与 Markdown 本地保存链路。

## 验证

- `npm run build` 通过。
