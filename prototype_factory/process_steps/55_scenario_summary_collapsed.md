# 2-55 场景识别上一步结果默认折叠

## 目标

第 02 步 `场景识别` 页面中，“来自需求输入阶段的拆解摘要”内容较长，默认展开会挤占主要工作区。将该区域改为默认折叠。

## 已完成

- `上一步结果` 区域默认收起。
- 标题行保留“上一步结果 / 来自需求输入阶段的拆解摘要”。
- 点击“展开查看”后显示业务背景、客户痛点、业务目标、用户角色、待确认问题与回复。
- 点击“收起”可再次折叠。

## 验证

- `npm run build` 通过。
