# 2-57 功能设计当前场景大卡片删除

## 目标

第 03 步 `功能设计` 页面中，当前场景大卡片与上方业务场景选择信息重复，占用空间较大。按要求删除该区域。

## 已完成

- 删除 `当前场景` 名称与描述大卡片。
- 保留并压缩展示关联角色、任务流程、建议页面三项承接信息。
- 页面继续保留业务场景选择、功能模块映射、大模型修改和确认进入下一步链路。

## 验证

- `npm run build` 通过。
