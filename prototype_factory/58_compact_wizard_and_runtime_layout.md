# 2-58 方法链路横向窄条与生成方式简化

## 目标

优化原型工厂主工作台布局：左侧纵向方法链路改为横向窄条；生成方式区域去掉大段说明，改为紧凑横向配置区。

## 已完成

- 方法链路从左侧大侧栏改为顶部横向步骤条。
- 步骤条只显示编号、步骤名称和状态，隐藏长说明，减少占用空间。
- 当前步骤工作台改为步骤条下方全宽展示。
- 生成方式区域去掉冗长说明，只保留生成方式、Key 状态、模式、模型。
- DeepSeek 配置表单保持横向紧凑布局。
- 响应式下步骤条自动换行。

## 验证

- `npm run build` 通过。
